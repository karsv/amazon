language: java
jdk:
  - openjdk11
script:
  - mvn clean install
cache:
  directories:
    - $HOME/.m2

before_install:
  - chmod +x mvnw

services:
  - postgresql

before_script:
  - psql -c 'create database travis_ci_test;' -U postgres